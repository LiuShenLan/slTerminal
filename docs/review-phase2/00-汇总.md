# slTerminal Phase 2 审查汇总——修复计划执行结果

## 1. 审查概览

- **日期**：2026-08-21
- **范围**：`docs/review-fix/` 修复计划的全部开发内容——19 个 Stage（S01~S19）+ 收尾 commit `a40ee09`；代码及文档，不含测试用例质量
- **方法**：7 个主题组串行静态审查（闭环判定 + 增量质量双查），主流程跑基线门禁与 L1/L2 测试
- **输入**：`docs/review-fix/checklist.md`（93 项）、`stages.md`、各 Stage commit diff、`docs/review/` 原报告、当前代码现状
- **分组报告**：`01-依赖升级.md` ~ `07-文档同步.md`

**判定口径**（四档）：

- **fixed**：修复落地且符合 checklist 修复要点
- **partial**：部分覆盖 / 要点未完整落地
- **not-fixed**：无实质变更
- **closed-by-decision**：按 D1~D11 决策 / 豁免登记关闭，登记存在且内容准确

**新问题分级口径**：沿用 Phase 1——安全/稳定性维度 P0 = 可导致崩溃、数据丢失、RCE、越界读取；CI 阻断视为 P0 等价（合流即红）。性能/文档类问题最高 P2。

## 2. 基线验证结果（2026-08-21 实跑）

| 命令 | 结果 |
|------|------|
| `npx tsc --noEmit` | ✅ 通过 |
| `npx eslint src/` | ✅ 通过 |
| `cargo clippy -- -D warnings` | ✅ 通过 |
| `cargo fmt -- --check` | ❌ **失败**：5 处格式偏差（`src-tauri/src/pty/shell.rs` 3 处、`src-tauri/src/pty/spawn.rs` 2 处，为 S02/S06 修复代码未过 rustfmt） |
| `npm test`（L2） | ✅ 154 文件 / 2620 用例全过 |
| `cargo test --test-threads=1`（L1） | ✅ 全部套件通过（退出码 0） |

> fmt 失败本身即为新发现问题（见 4.3 节 B-0），说明 Stage 门禁在收尾 commit 后未再全量复跑。

## 3. 闭环判定统计

### 3.1 分组矩阵

| 组 | fixed | partial | not-fixed | closed-by-decision | 小计 |
|----|------:|--------:|----------:|-------------------:|-----:|
| 01-依赖升级（TE-01~13） | 7 | 2 | 2 | 2 | 13 |
| 02-安全（SEC-01~08/10/12~14 + BE-01） | 10 | 1 | 0 | 2 | 13 |
| 03-异步与watcher（BE-02/04/10/11、SEC-08/14、FE-04） | 6 | 1 | 0 | 0 | 7 |
| 04-性能（BE-03/05/06/12/19 + FE-01/15~21/29/30/32~34） | 17 | 0 | 0 | 1 | 18 |
| 05-错误处理与DTO（BE-13~16/18、SEC-11、FE-02/03/05~14） | 16 | 2 | 0 | 0 | 18 |
| 06-稳定性与死代码（BE-07~09/17/20、FE-22~28/35） | 11 | 1 | 0 | 1 | 13 |
| 07-文档同步（DOC-01~10） | 10 | 0 | 0 | 0 | 10 |
| **表内合计** | **77** | **8** | **2** | **6** | **93** |

> 口径说明：03 组 SEC-08/SEC-14 与 02 组重复判定（两组结论一致，均为 fixed），去重后 90 项；SEC-09（CSP 保留+登记）、BE-21（read_dir 豁免登记）经 07 组附加登记核验判定 fixed、FE-31 判定 partial（登记点文件缺失），补齐至 **93 项全量**。上表已按去重后唯一口径计入。

### 3.2 总闭环率

| 判定 | 数量 | 占比 |
|------|-----:|-----:|
| fixed | 77 | 82.8% |
| closed-by-decision | 6 | 6.5% |
| partial | 8 | 8.6% |
| not-fixed | 2 | 2.2% |
| **有效闭环（fixed + closed）** | **83 / 93** | **89.2%** |

**Phase 1 的 9 项 P0 全部闭环**：8 项 fixed（TE-01、SEC-01、SEC-02、BE-01、BE-02、BE-03、BE-04、BE-05）+ 1 项 closed-by-decision（FE-01，D1 多实例保留 + 页数上限 + ADR-0009 登记）。

### 3.3 未闭环 / 部分闭环清单（10 项）

| 条目 | 判定 | 残余问题 | 详见 |
|------|------|----------|------|
| TE-12 | not-fixed | `npx knip --production` 退出码 1（38 unused files / 146 unused exports），「零误报」未达成 | 01 报告 |
| TE-13 | not-fixed | CI 三门禁已配置，但 knip 门禁当前无法通过（依赖 TE-12） | 01 报告 |
| TE-06 | partial | `@tauri-apps/plugin-dialog` npm 仍锁 2.7.1，落后 cargo 侧 2.7.2 | 01 报告 |
| TE-07 | partial | TS7 编译器实际生效，但主 `typescript` 字段仍指向 TS6 包装器，eslint 解析器与编译器版本漂移 | 01 报告 |
| SEC-04 | partial | nonce 生成/校验存在，但明文内联于 srcDoc，iframe 内脚本可提取伪造 | 02 报告 |
| BE-10 | partial | `notify_stop_watch` 已建，但项目/页面移除至空页面路径未调用，watcher 残留至 LRU 淘汰 | 03 报告 |
| FE-08 | partial | `keyboard.ts:37` 粘贴 `readText()` 失败仍 `.catch(() => {})` 静默 | 05 报告 |
| FE-10 | partial | Diff 面板右栏外部修改重载失败仅 `console.warn`，无提示条 | 05 报告 |
| FE-35 | partial | `terminalTabConfig` 死代码残留（仅测试消费） | 06 报告 |
| FE-31 | partial | 决策内容已写，但 ADR-0009 指向的 `src/panels/editor/CLAUDE.md` 不存在，登记点悬空 | 07 报告 |

## 4. 新发现问题

### 4.1 分级统计

| 组 | P0 | P1 | P2 | 小计 |
|----|---:|---:|---:|-----:|
| 01-依赖升级 | 1 | 2 | 2 | 5 |
| 02-安全 | 0 | 2 | 1 | 3 |
| 03-异步与watcher | 0 | 2 | 5 | 7 |
| 04-性能 | 0 | 2 | 3 | 5 |
| 05-错误处理与DTO | 0 | 0 | 6 | 6 |
| 06-稳定性与死代码 | 0 | 0 | 5 | 5 |
| 07-文档同步 | 0 | 0 | 5 | 5 |
| **合计** | **1** | **8** | **27** | **36** |

> 去重说明：06 组「`src/ipc/CLAUDE.md` 仍记录 `setFocus`」与 07 组 M2 为同一问题，去重后 **35 项**（P2 26）。另：05 组 N1/N3 分别是 FE-08/FE-10 partial 判定的残留细节，双轨记录、不重复计数。
> 基线发现的 fmt 失败（B-0）未计入上表，列于 4.3。

### 4.2 P0 / P1 清单（9 项）

| # | 级别 | 问题 | 位置 | 修改建议 |
|---|------|------|------|----------|
| 1 | **P0** | knip `--production` 未达零误报，CI 新门禁合流即红 | `.github/workflows/ci.yml:45-46`、`knip.json` | 补 `entry`/`ignoreExports` 覆盖注册表/副作用导入形态直至退出码 0；或 CI 改 `--dependencies` 窄化口径并同步修订 TE-12 表述 |
| 2 | P1 | TypeScript 7 依赖声明失真：主字段指向 TS6 包装器，`node_modules/.bin` 双包竞争，eslint 解析到 TS6 AST | `package.json:80`、`:59` | 主 `typescript` 直改 `^7.0.2` 并升级 `typescript-eslint` 至 TS7 兼容版；做不到则回退 TS6 并在 ADR 登记「双 TS 并存」为临时债务 |
| 3 | P1 | `@tauri-apps/plugin-dialog` npm 2.7.1 落后 cargo 2.7.2 | `package.json:39`、`src-tauri/Cargo.toml:23` | npm 侧升 2.7.2 刷新 lock，补 dialog 路径回归断言 |
| 4 | P1 | SEC-01 `paths_match` 字符串回退削弱白名单：canonicalize 失败时降级为字符串比对，可被 reparse point/执行别名绕过 | `src-tauri/src/pty/shell.rs:105-122` | fallback 改 `CreateFileW`+`GetFileInformationByHandleEx` 文件身份比对；或收窄为「两侧均失败且字符串完全相同」并登记残余风险；补 L1 拒绝用例 |
| 5 | P1 | SEC-04 nonce 明文内联 srcDoc，iframe 内脚本可提取伪造 `slterm_key`（当前可关页签；global 上下文扩高危命令即升级） | `src/panels/html/HtmlPanel.tsx:65-71`、`:125-135` | 承认 nonce 不防 iframe 内部脚本：限制 iframe 可触达命令集（仅低风险）、UI 提示威胁模型；记录于 `src/panels/CLAUDE.md` |
| 6 | P1 | `set_project_root` 无并发序列化：A→B 快速切换时慢 canonicalize 的 A 后写回，沙箱 root 与 activePage 错位 | `src-tauri/src/state.rs:254-272`、`src/stores/projects.ts:159-186` | 后端以 `tokio::sync::Mutex` 串行化整个 set_project_root_impl；或前端统一 await 并丢弃过期请求 |
| 7 | P1 | 项目/页面移除至空页面路径未 `stopWatch`，watcher 残留占 OS 句柄至 LRU 淘汰 | `src/workspace/Workspace.tsx:116-152`、`:238-258` | activePageId 置 null 的清理路径 / `onDeletePage`/`removeProject` 链中显式 `stopWatch(prevRootRef.current)` |
| 8 | P1 | `MAX_PAGES` 实现为「每项目 20 页」，与 D1「页面总数上限」契约不符，多项目下 Dockview 实例仍可无界增长 | `src/stores/projects.ts:107-115` | addPage 增全局页面总数校验，超限拒绝+toast；同步 L2 与 ADR |
| 9 | P1 | `projects.ts` `switchToPage` 在 store 内直接调 IPC，违反硬约束 #12（store 纯状态） | `src/stores/projects.ts:159-186` | `setProjectRoot` 调用上提至调用方（NavTree/Workspace/pageApis）；或登记豁免并规划迁移 |

### 4.3 P2 清单（26 项，去重后；基线项单列）

**基线**：B-0 `cargo fmt --check` 失败（`pty/shell.rs` 3 处、`pty/spawn.rs` 2 处）——跑 `cargo fmt` 即可，并复跑门禁确认。

| # | 问题 | 位置 | 来源组 |
|---|------|------|--------|
| 1 | WDIO 工具链同包多版本实例（`@wdio/globals` 9.31/9.29.1、`expect-webdriverio` 6/5.7、`webdriverio` 9.30.1/9.30.0） | `package-lock.json` | 01 |
| 2 | `json-schema-library` 9.x 与 11.x 双 major 并存（codemirror-json-schema 锁定 9.x，已知债务） | `package-lock.json` | 01 |
| 3 | SEC-05 user 层写入二次确认仅前端门控，后端无确认令牌/审计 | `hooks/claude/config.rs:271-295` | 02 |
| 4 | `notify_watch` 前置沙箱校验仍在 IPC worker 同步 canonicalize | `notify/mod.rs:310-318` | 03 |
| 5 | `switchToPageShared` setProjectRoot 失败仅 console.error，与 FE-04 三处 toast 不一致 | `src/workspace/pageApis.ts:49-73` | 03 |
| 6 | SEC-14 清空旧 root 在写锁中毒分支不生效 | `src-tauri/src/state.rs:283-288` | 03 |
| 7 | watcher 排除目录匹配大小写敏感，`Node_Modules`/`Target` 变体漏排 | `notify/mod.rs:196-203` | 03 |
| 8 | Workspace SEC-01 effect 未 await setProjectRoot 即 startWatch，沙箱竞态 | `src/workspace/Workspace.tsx:244-253` | 03 |
| 9 | FE-16 `projectIdForCwd` 语义由「首命中」变「最深前缀」，嵌套项目归属变化未固化为测试 | `useNavTree.ts:139-170` | 04 |
| 10 | FE-30 虚拟化后程序式选中视口外行无滚动跟随 | `FileTree.tsx:596-751` | 04 |
| 11 | `refreshSubtreeAt` 未处理目标目录已删除场景，残留空目录行至下次全量刷新 | `useFileTree.ts:224-298` | 04 |
| 12 | 关窗处理器 cleanup 静默吞错 | `src/ipc/window.ts:55-58` | 05 |
| 13 | Diff 面板保存失败 toast 直接拼接 err，未走 `getErrorMessage` | `DiffPanel.tsx:372-376` | 05 |
| 14 | 编辑器保存失败 toast 同上未统一解析 | `useCodeMirror.ts:178-181` | 05 |
| 15 | stores `loadFromDisk` 内部 `catch {}` 空块零日志 | `stores/fontSize.ts:64`、`keybindings.ts:71`、`sideBar.ts:122` | 05 |
| 16 | `terminalTabConfig` 死代码残留（=FE-35 partial 残留） | `src/panelRegistry.ts:73` | 06 |
| 17 | 面板级 ErrorBoundary 无重试/重置机制，一次错误永久占位 | `src/panelRegistry.ts:32-48` | 06 |
| 18 | 关窗序列 `ptyKillAll()` 未包总超时（逐 session 3s 串行可拖长关窗） | `src/App.tsx:156-163` | 06 |
| 19 | `waitFor` 轮询 setTimeout 在 abort 后仍触发一次 | `pageApis.ts:91-96`、`restoreSession.ts:48` | 06 |
| 20 | `src/panels/editor/CLAUDE.md` 缺失，ADR-0009 FE-31 登记点悬空（=FE-31 partial 残留） | `src/panels/editor/` | 07 |
| 21 | `src/ipc/CLAUDE.md` 仍登记已删除的 `setFocus` + `ipc-window-contract` 用例数过时（与 06 组去重合并） | `src/ipc/CLAUDE.md:24,44` | 07 |
| 22 | `ipc-agent-history-contract` 用例数文档 14 vs test-inventory 18 不符 | `src/ipc/CLAUDE.md:44` | 07 |
| 23 | `src/panels/CLAUDE.md` 4 处终端用例数与 test-inventory 不一致 | `src/panels/CLAUDE.md` 测试模式节 | 07 |
| 24 | `src/features/sideViews/CLAUDE.md` 用例数自相矛盾（54/53、38/40） | `src/features/sideViews/CLAUDE.md` | 07 |
| 25 | FE-08 残留：终端粘贴 `readText()` 失败静默（=FE-08 partial 残留） | `panels/terminal/keyboard.ts:35-38` | 05 |
| 26 | FE-10 残留：Diff 右栏外部修改重载失败不可感知（=FE-10 partial 残留） | `DiffPanel.tsx:478-491` | 05 |

## 5. 人工验证点汇总（12 项，标注不判定）

| # | 来源 | 验证点 | 静态可查证据 | 状态标注 |
|---|------|--------|--------------|----------|
| 1 | S01 | E2E 全量实跑（WDIO 9.30 链路） | WDIO 全栈 9.30.1 + 插件 1.3.0 双侧一致 | commit a40ee09 声明 E2E 9/9 全过 |
| 2 | S03 | 白名单后全功能实测 + E2E | build.rs/lib.rs/capabilities 三处 34 条一致 | 未见实测记录 |
| 3 | S05 | 大仓库事件量实测 | 排除集 + symlink 过滤 + 合并上限已落地，L1 覆盖 | 未见实测记录 |
| 4 | S06 | claude 高输出流畅度 + 滚轮 + kill | 微批/PeekNamedPipe 落地；pty/CLAUDE.md 要求实测滚轮 | a40ee09 修复终端黑屏/spawn，表明已做部分实测 |
| 5 | S07 | 10MB 大文件打开 | 分块+上限校验落地 | 未见实测记录 |
| 6 | S09 | 损坏 settings/projects toast | corrupted 通道端到端落地 | 未见实测记录 |
| 7 | S12 | WebGL 焦点切换 + 20 页上限 + ADR-0001 行为 | 挂载即加载/切换仅 flush；MAX_PAGES 落地（语义偏差见 P1#8） | a40ee09 修复 win10 黑屏，表明已做部分实测 |
| 8 | S13 | 面板崩溃隔离 + 关窗 PTY 全灭 | withPanelBoundary 全类型包裹；Registry kill + pty_kill_all + Job Object | 未见实测记录 |
| 9 | S15 | dockview 8 拖拽/分屏/旧布局恢复 + E2E | dockview-react 8.1.0；无显式 serde 适配代码，兼容性交由测试兜底 | E2E 9/9（a40ee09） |
| 10 | S16 | CI 三门禁实跑 | ci.yml 已配置；npm audit 本地 high=0；cargo audit 本地网络受限 | **knip 门禁当前不可过（P0#1）** |
| 11 | S17 | user 层确认弹窗实测 | confirmDialog 门控落地（仅前端，见 P2#3） | 未见实测记录 |
| 12 | S18 | 万级节点目录实测 | 虚拟化窗口 + L2 用例落地 | 未见实测记录 |

## 6. 与 Phase 1 对照总账

- Phase 1：130 条原始发现 → 去重 105 项 → checklist 合并为 93 项（映射见 `docs/review-fix/checklist.md` 第 6 节）
- Phase 2：93 项中 **83 项有效闭环（89.2%）**、8 项 partial、2 项 not-fixed；9 项 P0 全部闭环
- 修复新引入问题 **35 项**（去重后）：P0 1、P1 8、P2 26 + 基线 fmt 1 项
- 新增硬约束违反 1 处：约束 #12（store 内直接 IPC，`projects.ts:159-186`，P1#9）

**总体评价**：修复计划执行质量整体可靠——安全与资源无界两大类系统性风险已消除，DTO 契约与错误处理体系成形。残余风险集中在三处：① CI knip 门禁合流即红（P0，须先修）；② 两处安全修复的纵深不足（shell 白名单 fallback、iframe nonce 威胁模型）；③ 项目/页面切换链路的并发与生命周期竞态（set_project_root 乱序写回、空页面 watcher 残留）。建议优先处置 P0/P1 共 9 项后再进入测试用例专项 review。
