# 04-后端与配置 Review 报告

> 范围：tauri.conf.json / capabilities / ipc window wrapper / P1-19 关窗链路 / fail-safe 三处 / 新增依赖
> 严重度：P0 阻塞（链路断/权限失效致功能崩）｜P1 严重（配置错误/权限越界）｜P2 次要｜P3 建议

## 问题清单

（无）

## 核查覆盖说明

- **tauri.conf.json**：`app.windows[0]` 内 `"decorations": false`（TB-01）与 `"backgroundColor": "#0a0a0b"`（TH-06）位置正确，值与 `docs/ui-redesign-impl/checklist.md` 附录 A 一致；未覆盖/删除其它必要配置项。
- **src-tauri/capabilities/default.json**：Stage 04 补充的四权限标识符均为 Tauri 2 官方名称：`core:window:allow-minimize`、`core:window:allow-toggle-maximize`、`core:window:allow-close`、`core:window:allow-start-dragging`；无通配 `*`，未声明 scope 即默认最小范围；保留的 `core:window:allow-destroy` 与 `registerCloseHandler` 的 `destroy()` 兜底兼容。
- **src/ipc/window.ts**：`minimizeWindow` / `toggleMaximizeWindow` / `closeWindow` 三 wrapper 均为 `getCurrentWindow()` 标准 Promise 封装；`closeWindow` 使用 `close()` 而非 `destroy()`，Tauri 2 文档明确 `close()` 触发 `onCloseRequested` 事件，可复用 P1-19 链路。
- **P1-19 关窗链路**：`App.tsx:104-172` 通过 `registerCloseHandler` 注册回调，`TerminalRegistry.getAll()` 遍历所有已注册终端，对含 `sessionId` 的条目调用 `pty.kill(entry.sessionId, panelId)`，并设 `SHUTDOWN_TIMEOUT_MS = 3000`；`registerCloseHandler` 内部 `preventDefault()` + `finally` 中 `destroy()`；后端 `pty/spawn.rs` 仍对每个子进程使用 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`。`decorations: false` 后链路未被破坏。
- **fail-safe 三处一致性**：`index.html:10` body `background: #0a0a0b`；`src/main.tsx:28-31` 超时错误页 `background: #0a0a0b`、`color: #ece9e4`、强调色 `#d9706b`、字体栈为 `JetBrains Mono, Cascadia Mono, Consolas, Microsoft YaHei UI, monospace`；`tauri.conf.json:21` `backgroundColor: #0a0a0b`。三处完全一致。
- **新增依赖**：`package.json` 中 `@fontsource/jetbrains-mono ^5.3.0` 与 `lucide-react ^1.31.0` 均位于 `dependencies`；`package-lock.json` 已同步写入对应 resolved/integrity 条目；两版本在 npm registry 均存在，且 `lucide-react@1.31.0` peerDependencies 含 `react ^19.0.0`，与当前 React 19.2.7 兼容。
- **后端 Rust 源码改动**：`85cd412..HEAD` 内 `src-tauri/src/` 非空 diff，仅涉及 `agent_history` 模块新增 `agent_history_read_title` 命令（运行中会话标题通道）。已逐文件审查：
  - `src-tauri/src/lib.rs:111` 已在 `generate_handler!` 注册；
  - `src-tauri/src/agent_history/mod.rs` 新增 `AgentHistoryTitle` DTO，字段 `title`/`title_source` 使用 `#[serde(rename_all = "camelCase")]`，与 `src/types/agentHistory.ts` 双边对应；
  - `run_read_title` 先执行 `validate_session_id` 前置校验，再在 `spawn_blocking` 内读文件，符合硬约束 #3；
  - `claude/ops.rs::read_session_title` 文件缺失返回 `Ok(title: None)`，与 delete 的 Err 语义区分正确；
  - L1 测试新增 16 条覆盖回退链/缺失/非法 id/大文件/损坏等场景。
  - 实跑：`cargo test --manifest-path src-tauri/Cargo.toml agent_history -- --test-threads=1` 86 条通过；`cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings` 通过；`npx tsc --noEmit` 通过。
