# 06-文档同步 Review 报告

> 范围：CLAUDE.md 族 / CONTEXT.md / adr.md / requirements.md 注记 与代码现状一致性
> 严重度：P0 阻塞（文档指引会导致错误开发行为）｜P1 严重（描述与现状实质性矛盾）｜P2 次要（过时表述/残留引用）｜P3 建议

## 问题清单

### [P1] `src/stores/CLAUDE.md` 侧栏默认态仍停留在 NAV-05 之前
- 位置：`src/stores/CLAUDE.md:36`
- 描述：文档称 `DEFAULT_ZONES = {top:["projects","explorer","commit","agent-status"], bottom:[]}`、`DEFAULT_OPEN.top="projects"`。实际代码在 NAV-05 后已将三槽改为 `nav`/`explorer`/`commit`，并删除 `agent-status` 视图：`src/features/sideViews/sideBarState.ts:33-42` 定义 `DEFAULT_ZONES.top = ["nav","explorer","commit"]`、`DEFAULT_OPEN.top="nav"`。
- 修复建议：将该段更新为 `nav`/`explorer`/`commit`，删除 `agent-status`；同步说明默认打开导航树。

### [P2] `src/features/shortcuts/CLAUDE.md` hooks 配置入口描述未随决策 4 更新（清单外发现）
- 位置：`src/features/shortcuts/CLAUDE.md:126`
- 描述：文档称入口“已迁移到侧栏右键菜单”，依赖 `SidebarTree` 的「打开 Hooks 配置」菜单项。实际上 SidebarTree 已删除，入口唯一化为活动栏底部「配置」按钮：`src/features/sideViews/ActivityBar.tsx:248-267`、sideViewDefs 注释 `src/features/sideViews/sideViewDefs.ts:9-10`、入口函数 `src/features/hooksConfig/openHooksConfig.ts`。
- 修复建议：改为“活动栏底部固定「配置」按钮 → `openHooksConfigFromActivityBar()`”，删除对 SidebarTree 菜单项的引用。

### [P2] `src/ipc/CLAUDE.md` 窗口 wrapper 数量与实际不符
- 位置：`src/ipc/CLAUDE.md:24`
- 描述：文档称 `window.ts` 有“六个 wrapper”，但随后列出并实际导出 7 个函数：`onFocusChanged`/`requestUserAttention`/`setFocus`/`registerCloseHandler`/`minimizeWindow`/`toggleMaximizeWindow`/`closeWindow`（`src/ipc/window.ts:5-102`）。
- 修复建议：修正为“七个 wrapper”或按语义分组（通用 4 个 + 标题栏 3 个）。

### [P2] `src/panels/CLAUDE.md` 文件表遗漏 `HooksConfigPanel` 导出
- 位置：`src/panels/CLAUDE.md:270`
- 描述：`index.ts` 表仅写导出 `TerminalPanel、EditorPanel、HtmlPanel、GitShowPanel、DiffPanel`，但 `src/panels/index.ts:6` 同时导出了 `HooksConfigPanel`，且前文面板类型列表包含 `hooksConfig`。
- 修复建议：在文件表补充 `HooksConfigPanel`。

### [P3] `src/theme/CLAUDE.md` `tauri.conf.json` 行号引用错误
- 位置：`src/theme/CLAUDE.md:64`（并 `src/theme/schemes/linear.ts:9`）
- 描述：文档称启动链 fail-safe 三处之一为 `src-tauri/tauri.conf.json:20 window backgroundColor`，但 `backgroundColor` 实际位于 `src-tauri/tauri.conf.json:21`。
- 修复建议：行号改为 `:21`。

### [P3] `src/workspace/CLAUDE.md` `index.ts` 文件表导出项不完整
- 位置：`src/workspace/CLAUDE.md:44`
- 描述：文件表只列 `Workspace`、`panelRegistry`、`PANEL_TYPES`、`saveLayout`/`loadLayout`，但 `src/workspace/index.ts:5-13` 还导出 `PANEL_TERMINAL`、`PANEL_EDITOR`、`PANEL_HTML_VIEWER`、`FILE_PANEL_TYPES`、`isValidPanelType`、`isAlwaysRenderPanel` 等。
- 修复建议：补充或改为“含面板类型常量与辅助函数”的概括性描述。

## 核查覆盖说明

- 已逐一阅读 DOC-01~06 列出的全部文档，并对照对应源码文件（theme/sideViews/sidebar/agentStatus/agentHistory/workspace/lib/panels/ipc/navTree/titleBar）及根 CLAUDE.md、CONTEXT.md、adr.md、requirements.md。
- 残留扫描：`src/**/CLAUDE.md` 无 `darcula` 残留；`AgentStatusView`/`AgentStatusRow`/`AgentHistorySections`/`SidebarTree` 引用均为“已删除/已退役”历史说明，无现役式描述。
- 未重复报告测试维度（test-inventory.md、e2e-tests/CLAUDE.md）问题。
