# Phase 3 v4 审计发现问题

> 审计日期：2026-06-28
> 审计范围：`6.结果验收报告-v4.md` T1–T8 自动化门
> 方法：全量重跑 T1–T8，与报告数字逐项对照

---

## T8：E2E 报告作假——实测 3/3 失败，非 1/1 PASS

**报告声称**（§1.1 T8）：
```
Spec Files:  1 passed, 1 total (100% completed) in 00:00:35
```
含具体检查点：标题、Dockview API 就绪、PTY spawn、`e2e_marker`、`.xterm` 容器、`writeClipboard`、`e2e_paste_verify`。

**实测**（2026-06-28，`b0560cb` 全量构建后 `npm run wdio`）：
```
Spec Files:  0 passed, 1 failed, 1 total (100% completed) in 00:00:28
"DotReporter" Reporter: FFF
```

**失败根因**：`window.__dockviewApi` 在整个测试窗口内始终 `undefined`。

**调用链追踪**：
1. 测试第一步 `__slterm_e2e_createProject('C:\\e2e-test')`（`App.tsx:22-49`）正确创建项目+页面、写入 Zustand store、设置 `activePageId`
2. 测试轮询 `window.__dockviewApi`（10s timeout，L23-31）→ 每轮返回 `false` → 最终超时失败
3. `window.__dockviewApi` 仅在 `Workspace.tsx:301` 设置：`handlePageApiReady` 回调中 `window.__dockviewApi = api`
4. `handlePageApiReady` 由 PageDockview 的 Dockview `onReady` 事件触发
5. PageDockview 需要 `initializedPages.has(pageId)` 为 true 才渲染（`Workspace.tsx:334`）
6. `ensurePageInitialized` 通过 `useEffect` 在 `activePageId` 变化时调用（`Workspace.tsx:318-320`）

**可能的中间断裂点**（需进一步排查）：
- Zustand store 更新 → React 重渲染的异步时序：`createProject` 同步调用 `setActivePage` 后，React 尚未提交渲染
- `initializedPages` 状态更新与 PageDockview 渲染之间的竞态
- Dockview 组件在 blank state（无 layout 的页面）下不触发 `onReady`——`makeDefaultLayout` 生成的 layout 可能不符合 Dockview 预期

**报告证据链可信度**：报告列出 7 条具体证据（标题、Dockview API、PTY spawn、e2e_marker、.xterm、writeClipboard、e2e_paste_verify），含终端输出文本。当前代码无论何种启动状态都无法在 28s 超时窗口内让 `__dockviewApi` 出现，报告声称的 RESULT true 输出与实测 RESULT false 矛盾。可能来源：① 从未在此 commit 上测试过 ② 测试环境差异（如本地有残留 localStorage 恢复布局）③ 报告凭历史输出编写。

---

## 其他自动化门（T1–T7）：全部真实通过

| 门 | 报告数字 | 实测数字 | 一致 |
|----|---------|---------|------|
| T1 — cargo test | 116 (109+3+4) | 109+3+4=116 | ✅ |
| T2 — npm test | 401 | 401 | ✅ |
| T3 — tsc | exit 0 | exit 0 | ✅ |
| T4 — eslint | exit 0 | exit 0 | ✅ |
| T5 — clippy | exit 0 | exit 0 | ✅ |
| T6 — cargo build | exit 0 | exit 0 | ✅ |
| T7 — tauri build | exit 0 | exit 0 | ✅ |
