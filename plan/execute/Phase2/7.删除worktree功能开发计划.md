# 删除 worktree 功能开发计划

> 范围：删除所有 worktree 相关代码，侧栏从三级树改为二级树（项目 → 操作页面），移除 git 仓库门槛
> 前置：用户确认 worktree 功能不需要，任意文件夹均可添加为项目

---

## 1. 删除文件清单（10 个）

| # | 文件 | 原因 |
|---|------|------|
| D1 | `src-tauri/src/git/worktree.rs` | worktree 全部逻辑：list/add/remove 命令 + 测试 |
| D2 | `src-tauri/src/git/mod.rs` | git 模块仅含 worktree 子模块 + git2 读操作，全部不再需要 |
| D3 | `src/features/worktree/WorktreeActions.tsx` | CreateWorktreeDialog + DeleteWorktreeConfirm |
| D4 | `src/features/worktree/index.ts` | barrel export |
| D5 | `src/features/worktree/` 目录 | 上述两文件删除后为空目录 |
| D6 | `src/ipc/git.ts` | isRepo/listWorktrees/addWorktree/removeWorktree 全部不再需要 |
| D7 | `src/types/git.ts` | WorktreeInfo + WorktreeBinding 类型定义 |
| D8 | `src/__tests__/git-ipc.test.ts` | git IPC mock 测试（6 个测试用例） |
| D9 | `src/panels/terminal/TerminalRegistry.ts` | 仅含 N2 修复草案代码，未接入主流程；getByWorktree 与 worktree 耦合 |
| D10 | `src/features/sidebar/SidebarTree.tsx` 中 worktree 组件 | 删除 `WorktreeRow` 组件 + `handleDeleteWorktree`/`handleNewWorktree` 回调 + worktree 对话框状态 |

---

## 2. 修改文件清单（21 个）

### 2.1 后端（4 个）

| # | 文件 | 行号 | 改动 |
|---|------|------|------|
| M1 | `src-tauri/src/lib.rs` | 70-74 | 删除 `git::git_is_repo`、`git::git_root`、`git::worktree::git_worktree_list`、`git::worktree::git_worktree_add`、`git::worktree::git_worktree_remove` 注册 |
| M2 | `src-tauri/src/error.rs` | 22 | 删除 `Worktree(String)` 变体 |
| M3 | `src-tauri/Cargo.toml` | 31, 39-40 | 删除 `git2 = "0.21.0"`、`wait-timeout = "0.2"`、`dirs = "6"` 依赖 |
| M4 | `.gitignore` | 36-37 | 删除 `.claude/worktrees/` 行及注释 |

### 2.2 侧栏（1 个，改动最大）

| # | 文件 | 行号 | 改动 |
|---|------|------|------|
| M5 | `src/features/sidebar/SidebarTree.tsx` | 多处 | 见下方详细说明 |

**M5 详细改动**：

| 位置 | 改动 |
|------|------|
| 第 3 行 | 注释从"L1→L2→L3"改为"L1→L2" |
| 第 13-14 行 | 删除 `CreateWorktreeDialog`、`DeleteWorktreeConfirm`、`WorktreeInfo`、`WorktreeBinding` 导入 |
| 第 178-218 行 | **删除 `WorktreeRow` 组件**（41 行） |
| 第 275-277 行 | 删除 `pageWorktreePath` 函数 |
| 第 307 行 | 删除 `removeWorktree` 解构 |
| 第 319-325 行 | 删除 worktree 对话框状态（`worktreeDialogProject`、`deleteTarget`） |
| 第 328-340 行 | 删除 `handleNewWorktree`、`handleWorktreeCreated` |
| 第 357-358 行 | **删除 `git.isRepo()` 检查**——任意文件夹直接创建项目 |
| 第 365-392 行 | **重写**：不再调用 `git.listWorktrees`，直接创建默认操作页面（cwd=所选目录路径，带默认终端布局） |
| 第 404-410 行 | 删除 `handleNewPage` 的 `WorktreeInfo` 参数——改为 `(projectId: string, cwd: string)` |
| 第 446-488 行 | 删除 `handleDeleteWorktreeConfirm` |
| 第 552 行 | 删除右键菜单"新建工作树"选项 |
| 第 568-607 行 | **重写**：二级树渲染——`ProjectRow` → `PageRow`，去掉 `WorktreeRow` 层 |
| 第 658-677 行 | 删除 `CreateWorktreeDialog` 和 `DeleteWorktreeConfirm` 渲染 |
| 全局 | `OperationPage.binding?.worktreePath` → `OperationPage.cwd` |

### 2.3 Store 层（3 个）

| # | 文件 | 行号 | 改动 |
|---|------|------|------|
| M6 | `src/stores/projects.ts` | 8 | 删除 `WorktreeInfo`、`WorktreeBinding` 导入 |
| M6 | `src/stores/projects.ts` | 17 | `worktrees: WorktreeInfo[]` → 删除字段 |
| M6 | `src/stores/projects.ts` | 27 | `binding?: WorktreeBinding` → `cwd?: string` |
| M6 | `src/stores/projects.ts` | 55-56 | 删除 `addWorktree`、`removeWorktree` 类型声明 |
| M6 | `src/stores/projects.ts` | 100-139 | 删除 `addWorktree`、`removeWorktree` 实现 |
| M7 | `src/stores/sessions.ts` | 12-13 | 删除 `worktreeId` 字段 |
| M7 | `src/stores/sessions.ts` | 52-63 | 删除 `getSessionsByWorktree`、`getPanelIdsByWorktree` |
| M8 | `src/stores/index.ts` | 1 | 删除 `getSessionsByWorktree, getPanelIdsByWorktree` 导出 |

### 2.4 Workspace 层（2 个）

| # | 文件 | 行号 | 改动 |
|---|------|------|------|
| M9 | `src/workspace/Workspace.tsx` | 26 | 删除 `WorktreeBinding` 导入 |
| M9 | `src/workspace/Workspace.tsx` | 37-47 | 重写 `getActivePageCwd()`：从 `page.binding.worktreePath` 改为 `page.cwd` |
| M9 | `src/workspace/Workspace.tsx` | 69,73,92,96,121,125 | `params: { panelId: id, binding }` → `params: { panelId: id, cwd }` |
| M9 | `src/workspace/Workspace.tsx` | 5 | 注释从"携带当前页面 binding"改为"携带当前页面 cwd" |
| M10 | `src/workspace/panelRegistry.ts` | 8,13,16 | 删除 `WorktreeBinding` 导入，`binding?: WorktreeBinding` → `cwd?: string` |

### 2.5 终端面板层（2 个）

| # | 文件 | 行号 | 改动 |
|---|------|------|------|
| M11 | `src/panels/terminal/TerminalPanel.tsx` | 10 | 删除 `WorktreeBinding` 导入 |
| M11 | `src/panels/terminal/TerminalPanel.tsx` | 16-17 | `binding?: WorktreeBinding` → `cwd?: string` |
| M11 | `src/panels/terminal/TerminalPanel.tsx` | 40-41 | `params.binding?.worktreePath` → `params.cwd` |
| M12 | `src/panels/terminal/useXterm.ts` | 33 | 注释"来自 worktree binding" → "来自操作页面 cwd" |

### 2.6 类型与导出层（4 个）

| # | 文件 | 行号 | 改动 |
|---|------|------|------|
| M13 | `src/types/pty.ts` | 15-16 | 删除 `worktree?: string` 字段 |
| M14 | `src/types/index.ts` | 3 | 删除 `export type { WorktreeInfo, WorktreeBinding } from "./git"` |
| M15 | `src/features/index.ts` | 3-6 | 删除 `CreateWorktreeDialog, DeleteWorktreeConfirm` 导出 |
| M16 | `src/ipc/index.ts` | — | 如有 `git` 导出则删除 |

### 2.7 测试文件（3 个）

| # | 文件 | 改动 |
|---|------|------|
| M17 | `src/__tests__/projects.test.ts` | 删除 `WorktreeInfo`/`WorktreeBinding` 导入 + `makeWorktree` 函数 + worktree 测试用例（`addWorktree`×2、`removeWorktree`×1）；`makeProject` 中移除 `worktrees` 字段 |
| M18 | `src/__tests__/layout-switch.test.ts` | 第 12 行删除 `worktrees: []`；第 79-100 行删除 binding 测试 |
| M19 | `src/__tests__/workspace.test.tsx` | 第 50 行删除 `worktrees: []` |

---

## 3. 汇总

| 类别 | 数量 | 文件 |
|------|------|------|
| 整个删除 | 8 + 1 目录 + 1 组件 | `git/worktree.rs`、`git/mod.rs`、`WorktreeActions.tsx`、`worktree/index.ts`、`ipc/git.ts`、`types/git.ts`、`git-ipc.test.ts`、`TerminalRegistry.ts`、`worktree/` 目录、`WorktreeRow` 组件 |
| 少量修改（1-5 行） | 14 | `lib.rs`、`error.rs`、`Cargo.toml`、`.gitignore`、`sessions.ts`、`index.ts`×2、`panelRegistry.ts`、`TerminalPanel.tsx`、`useXterm.ts`、`pty.ts`、`types/index.ts`、`features/index.ts`、`workspace.test.tsx` |
| 较大修改 | 3 | `SidebarTree.tsx`（去掉 worktree 层）、`projects.ts`（数据模型）、`Workspace.tsx`（binding→cwd） |
| 测试调整 | 2 | `projects.test.ts`、`layout-switch.test.ts` |

**总计：删除 9 文件，修改 19 文件。**
