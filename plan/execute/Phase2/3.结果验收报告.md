# Phase 2 结果验收报告 —— 未通过项

> 审计日期：2026-06-21（v2，基于用户补充修改后的 `2.结果验收报告.md` 重新验证）
> 审计方法：T1–T7 全部实测复现；G1–G8 交叉验证；人工验收采纳用户记录
> 本报告仅列未通过项，不分析原因

---

## 1. 自动化验收 —— 审计验证结论

### 1.1 T1–T8 实测复现结果

| # | 命令 | 报告声称 | 实测结果 | 结论 |
|---|------|---------|---------|------|
| T1 | `cargo test -- --test-threads=1` | 24 passed, 0 failed | 19 (unittest) + 0 (main) + 1 (lib_crate) + 4 (pty_int) = **24 passed, 0 failed** | ✅ 一致 |
| T2 | `npm test` | 11 files, 57 tests | **11 passed (11), 57 passed (57)** | ✅ 一致 |
| T3 | `npm run test:l3` | 2 files, 5 tests | **2 passed (2), 5 passed (5)** | ✅ 一致 |
| T4 | `npx tsc --noEmit` | exit 0 | exit 0 | ✅ 一致 |
| T5 | `npx eslint src/` | exit 0 | exit 0 | ✅ 一致 |
| T6 | `cargo clippy -- -D warnings` | exit 0 | exit 0 | ✅ 一致 |
| T7 | `cargo build` | exit 0 | exit 0 | ✅ 一致 |
| T8 | `npm run wdio` | 1 passed, 1 total | 报告已附实测输出，`tauri build --debug --no-bundle` 后 spec PASS | ✅ 采纳报告证据 |

### 1.2 Phase 2 L1/L2 测试补全确认

| 计划要求（§5.4 DoD） | 状态 | 实测证据 |
|---------------------|------|---------|
| `test_worktree_list_add_remove` | ✅ 已补全 | `git::worktree::test_worktree_list_add_remove ... ok` |
| `test_worktree_remove_blocked_when_open` | ✅ 已补全 | `git::worktree::test_worktree_remove_blocked_when_open ... ok` |
| `projects.test.ts`（8 tests） | ✅ 已创建 | `addProject/removeProject/addWorktree/dedup/removeWorktree/addPage/removePage/switchToPage` 全部 PASS |
| `layout-switch.test.ts`（5 tests） | ✅ 已创建 | 初始状态/setActivePage/setLayoutSwitching/switchToPage/binding 全部 PASS |
| `git-ipc.test.ts`（6 tests） | ✅ 已创建 | `git_is_repo`/`git_root`/`git_worktree_list`/`add`/`remove` mockIPC 全部 PASS |

**自动化验收结论：T1–T8 全部通过，G1–G8 全部通过，Phase 2 L1/L2 测试全部补全并 PASS。**

---

## 2. 人工验收 —— 未通过项

| # | 检查项 | 结果 | 说明 |
|---|--------|------|------|
| H1 | 侧栏三级树展示 | ❌ | 非 git 仓库选择后无任何反馈，点击确认仍显示空白态；选择 git 仓库时可以正常显示 |
| H2 | 手动添加项目 | ✅ | 选择 slTerminal 源码目录后正常显示 |
| H3 | 新建 worktree | ❌ | 右键 worktree 仅有"新建操作页面"与"新建工作树"两个选项；项目上右键无任何反应 |
| H4 | 删除 worktree | ❌ | 无法验证（依赖 H3） |
| H5 | 操作页面切换 | ❌ | 无法验证（依赖 H3/H4） |
| H6 | 重启不恢复 PTY | ❌ | 关闭应用后重启，侧边栏为空白态，项目列表和操作页面结构未恢复 |
| H7 | Phase 1 回归 | ✅ | 终端可用，编辑器语法高亮正常，页签可拖拽分屏 |
| H8 | 终端 cwd 正确 | ❌ | 无法验证（依赖 H3） |

---

## 3. 用户发现的额外问题

| # | 问题 | 严重程度 |
|---|------|---------|
| B1 | 应用启动时自动打开终端，但此时项目栏为空白态——应在启动时展示空白操作页面 | 🔴 行为与规格偏离 |
| B2 | worktree 下仅有一个操作页面时，点击侧栏操作页面行 → 页面显示空白态 → 再次点击 → 恢复 → 再次点击 → 又空白（循环 bug） | 🔴 布局切换逻辑缺陷 |
| B3 | 仅支持 git 仓库项目——非 git 仓库选择后无任何反馈，且无法在非仓库目录下创建操作页面 | 🟡 功能缺口 |

---

## 4. 汇总

| 类别 | 总数 | 通过 | 未通过 | 无法验证 |
|------|------|------|--------|---------|
| T1–T8 自动化门 | 8 | 8 | 0 | 0 |
| G1–G8 源码审查 | 8 | 8 | 0 | 0 |
| Phase 2 L1 测试（DoD） | 3 | 3 | 0 | 0 |
| Phase 2 L2 测试（DoD） | 3 | 3 | 0 | 0 |
| H1–H8 人工体验 | 8 | 2（H2, H7） | 6 | — |
| 额外问题 | 3 | — | 3 | — |

**剩余缺口（全部在人工验收层）**：
1. H1：非 git 仓库项目无反馈
2. H3：新建 worktree UI 流程不通（右键菜单缺失 + 项目右键无反应）
3. H4/H5/H8：因 H3 阻塞无法验证
4. H6：项目列表未持久化恢复（重启后空白）
5. B1：启动时不应自动打开终端
6. B2：操作页面点击切换循环空白 bug
7. B3：不支持非 git 仓库项目
