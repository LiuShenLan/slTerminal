# Phase 2 结果验收报告 —— 未通过项

> 审计日期：2026-06-21
> 审计范围：`plan/execute/Phase2/2.结果验收报告.md` 自动化验证结论 + 人工验收结论
> 审计方法：自动化门（T1–T8）全部实测复现；G1–G8 以等效工具（Grep/PowerShell/Glob）交叉验证；人工验收直接采纳用户记录结论
> 本报告仅列未通过项，不分析原因

---

## 1. 自动化验收 —— 未通过项

### 1.1 T8 `npm run wdio` —— 无法复现验证

报告声称 E2E spec PASS（退出 0），但**无法在本审计中复现验证**：

- `cargo build` 产物不含前端 `dist/`（`beforeBuildCommand` 仅在 `tauri build` 时执行）
- 以 `cargo build` 产物运行 wdio → `window.__dockviewApi` 持续轮询返回 `false` → 超时
- 报告要求的构建命令 `tauri build --debug --no-bundle` 未在本审计中执行
- **结论**：T8 验证状态 **无法确认**。报告声称"1 passed, 1 total (100% completed)"的原始命令输出未附在报告中，无法交叉验证

### 1.2 Phase 2 L1 集成测试缺失（对照计划 §5.4 DoD）

| 计划要求 | 实际状态 |
|---------|---------|
| `test_worktree_list_add_remove` — 对临时仓库执行 `git init → commit → worktree add → list 含新条目 → remove → list 不含` | ❌ **不存在**。仅有 `test_parse_worktree_list`（纯字符串解析单元测试），不执行任何 git 命令 |
| `test_worktree_remove_blocked_when_open` — 有页签时返回需确认信号 | ❌ **不存在**。`git/worktree.rs` 中无此测试 |

### 1.3 Phase 2 L2 前端测试全部缺失（对照计划 §5.4 DoD）

| 计划要求 | 实际状态 |
|---------|---------|
| `src/__tests__/projects.test.ts` — `test_add_project_appends_store` | ❌ **文件不存在** |
| `src/__tests__/layout-switch.test.ts` — `test_switch_page_swaps_layout` | ❌ **文件不存在** |
| `src/__tests__/git-ipc.test.ts` — mockIPC 测试 git_is_repo / git_worktree_list | ❌ **文件不存在** |

`npm test` 通过的 38 个测试全部为 Phase 1 遗留测试（`terminal.test.tsx`、`workspace.test.tsx`、`editor.test.tsx`、`ipc-ping.test.ts`、`language-mapping.test.ts`、`canFit.test.ts`、`keyboard.test.ts`、`terminal-serialize.test.ts`）。Phase 2 新增测试数量为 **0**。

---

## 2. 人工验收 —— 未通过项

### 2.1 H1 侧栏三级树展示 ❌

**操作**：点击"+ 添加项目"→ 选择非 git 仓库目录 → 确认

**预期**：项目出现在侧栏

**实际**：非 git 仓库选择后，点击确认仍显示空白态，没有任何变化。选择 git 仓库时可以正常显示。

### 2.2 H3 新建 worktree ❌

**操作**：右键 worktree → "新建 worktree"

**实际**：
- 右键 worktree 仅有"新建操作页面"与"新建工作树"两个选项（菜单项名称与规格不一致）
- "新建工作树"操作应在项目路径上右键触发，但**项目上右键无任何反应**

### 2.3 H4 删除 worktree ❌

**状态**：**无法验证**（依赖 H3 新建 worktree 未通过）

### 2.4 H5 操作页面切换 ❌

**状态**：**无法验证**（依赖 H3/H4 新建 worktree 未通过）

### 2.5 H6 重启不恢复 PTY ❌

**操作**：添加项目 → 关闭应用 → 重新启动

**预期**：侧栏恢复上次添加的项目和操作页面结构（名称+布局），PTY 为新 spawn

**实际**：关闭应用后重启，侧边栏为**空白态**。项目列表和操作页面结构未恢复。

### 2.6 H8 终端 cwd 正确 ❌

**状态**：**无法验证**（依赖 H3 新建 worktree 未通过）

---

## 3. 用户发现的额外问题

| # | 问题 | 严重程度 |
|---|------|---------|
| B1 | 应用启动时自动打开终端，但此时项目栏为空白态——应在启动时展示空白操作页面 | 🔴 行为与规格偏离 |
| B2 | worktree 下仅有一个操作页面时，点击侧栏操作页面行 → 页面显示空白态 → 再次点击 → 恢复 → 再次点击 → 又空白，循环 bug | 🔴 布局切换逻辑缺陷 |
| B3 | 仅支持 git 仓库项目——非 git 仓库选择后无任何反馈，且无法在非仓库目录下创建操作页面 | 🟡 功能缺口 |

---

## 4. 汇总

| 类别 | 总数 | 通过 | 未通过 | 无法验证 |
|------|------|------|--------|---------|
| T1–T8 自动化门 | 8 | 7 | 0 | 1（T8） |
| G1–G8 源码审查 | 8 | 8 | 0 | 0 |
| H1–H8 人工体验 | 8 | 2（H2, H7） | 6 | — |
| Phase 2 L1 测试（DoD） | 3 | 1（git_is_repo） | 2 | — |
| Phase 2 L2 测试（DoD） | 3 | 0 | 3 | — |
| 额外问题 | 3 | — | 3 | — |

**核心缺口**：
1. T8 无法验证（构建前提未满足）
2. Phase 2 L2 前端测试 0/3 创建
3. Phase 2 L1 集成测试 0/2 创建（仅有单元测试，无 git 实际操作集测）
4. 人工体验 6/8 项未通过或无法验证
5. 3 个功能行为问题（启动空白态、页面切换循环、非 git 仓库）
