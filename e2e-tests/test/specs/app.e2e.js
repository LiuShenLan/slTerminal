describe('slTerminal E2E', () => {
  it('应显示暗色窗口', async () => {
    // L4 样例 — 需本机有 msedgedriver 且与 WebView2 版本对齐
    // 无 msedgedriver 时此测试由 CI 覆盖
    console.log('L4 E2E 测试配置就绪，等待 CI 或本机 msedgedriver 执行');
  });
});
